# Data Output

Folder ini akan terisi setelah menjalankan:

```bash
python scripts/fetch_github_healthcare_repos.py --target 1200
```

Output:
- `github_healthcare_repositories.csv`
- `github_healthcare_summary.md`

Catatan: pada environment saat ini akses ke GitHub API diblokir oleh proxy (403), sehingga output aktual belum bisa digenerate di sini.
